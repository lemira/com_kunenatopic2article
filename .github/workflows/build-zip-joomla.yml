name: Build Joomla Package
on:
  workflow_dispatch:
    inputs:
      auto_download:
        description: 'Автоматически скачать файл после сборки'
        required: false
        default: 'true'
        type: boolean

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Set timestamp variable (Берлинское время)
        run: |
          TIMESTAMP=$(TZ="Europe/Berlin" date +"%y%m%d-%H%M")
          echo "Используемый TIMESTAMP: $TIMESTAMP"
          echo "TIMESTAMP=$TIMESTAMP" >> $GITHUB_ENV
          
      - name: Prepare component files
        run: |
          # Создаем временную директорию для подготовки файлов
          mkdir -p build_temp
          
          # Показываем текущую структуру репозитория
          echo "=== Структура репозитория ==="
          find . -maxdepth 3 -type f -not -path './.git*' -not -path './.github*' | sort
          echo "=========================="
          
          # Копируем все файлы кроме служебных
          rsync -av --exclude='.git*' --exclude='.github*' --exclude='build_temp*' --exclude='*.zip' . build_temp/
          
          # Показываем структуру для отладки
          echo "=== Структура файлов в build_temp ==="
          find build_temp -type f | sort
          echo "=== Всего файлов: $(find build_temp -type f | wc -l) ==="
          
          # Проверяем наличие ключевых файлов компонента
          echo "=== Проверка ключевых файлов ==="
          if [ -f "build_temp/kunenatopic2article.php" ]; then
            echo "✅ kunenatopic2article.php найден"
            ls -la build_temp/kunenatopic2article.php
          else
            echo "❌ kunenatopic2article.php НЕ найден"
          fi
          
          if [ -f "build_temp/kunenatopic2article.xml" ]; then
            echo "✅ kunenatopic2article.xml найден"
            ls -la build_temp/kunenatopic2article.xml
          else
            echo "❌ kunenatopic2article.xml НЕ найден"
          fi
          
      - name: Create ZIP archive with timestamp
        run: |
          cd build_temp
          zip -r ../Joomla-Component-${{ env.TIMESTAMP }}.zip . -x ".*"
          cd ..
          
          # Проверяем размер и содержимое ZIP
          echo "=== Информация о созданном ZIP ==="
          ls -lh Joomla-Component-${{ env.TIMESTAMP }}.zip
          echo "=== Содержимое ZIP (первые 20 файлов) ==="
          unzip -l Joomla-Component-${{ env.TIMESTAMP }}.zip | head -25
          
      - name: Upload ZIP as artifact
        uses: actions/upload-artifact@v4
        with:
          name: Joomla-Component-${{ env.TIMESTAMP }}
          path: Joomla-Component-${{ env.TIMESTAMP }}.zip
          retention-days: 30
          
      # Автоматическое создание временного релиза для быстрого скачивания
      - name: Create temporary release for download
        if: inputs.auto_download == true || inputs.auto_download == ''
        uses: softprops/action-gh-release@v2
        with:
          tag_name: temp-build-${{ env.TIMESTAMP }}
          name: "🔧 Temp Build ${{ env.TIMESTAMP }}"
          body: |
            **Временная сборка для скачивания**
            
            Этот релиз будет автоматически удален через 24 часа.
            
            ⏰ Время сборки: ${{ env.TIMESTAMP }}
            🔗 Коммит: ${{ github.sha }}
            
            📥 **Скачать файл можно прямо отсюда ↓**
          files: Joomla-Component-${{ env.TIMESTAMP }}.zip
          prerelease: true
          draft: false
          fail_on_unmatched_files: false
          generate_release_notes: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: true
          
      # Показать прямую ссылку для скачивания
      - name: Generate download link
        if: success()
        run: |
          echo "## 📥 Файл готов к скачиванию!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Способы скачивания:" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "1. **[📦 Скачать из Artifacts](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "2. **[🔗 Проверить Releases](${{ github.server_url }}/${{ github.repository }}/releases)** (если создание релиза прошло успешно)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📄 **Имя файла:** \`Joomla-Component-${{ env.TIMESTAMP }}.zip\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "🕐 **Время сборки:** ${{ env.TIMESTAMP }}" >> $GITHUB_STEP_SUMMARY
        
  # Job для автоматической очистки временных релизов (упрощенная версия)
  cleanup:
    needs: build
    runs-on: ubuntu-latest
    if: success()
    steps:
      - name: Cleanup old temporary releases
        uses: actions/github-script@v7
        continue-on-error: true
        with:
          retries: 3
          retry-exempt-status-codes: 400,401,403,404
          script: |
            try {
              const { data: releases } = await github.rest.repos.listReleases({
                owner: context.repo.owner,
                repo: context.repo.repo,
                per_page: 50
              });
              
              const now = new Date();
              const oneDayAgo = new Date(now.getTime() - 24 * 60 * 60 * 1000);
              
              let deletedCount = 0;
              
              for (const release of releases) {
                if (release.tag_name.startsWith('temp-build-') && 
                    new Date(release.created_at) < oneDayAgo) {
                  
                  try {
                    console.log(`Удаляем старый временный релиз: ${release.tag_name}`);
                    
                    // Удаляем релиз
                    await github.rest.repos.deleteRelease({
                      owner: context.repo.owner,
                      repo: context.repo.repo,
                      release_id: release.id,
                    });
                    
                    // Пауза между запросами
                    await new Promise(resolve => setTimeout(resolve, 1000));
                    
                    // Удаляем тег
                    await github.rest.git.deleteRef({
                      owner: context.repo.owner,
                      repo: context.repo.repo,
                      ref: `tags/${release.tag_name}`,
                    });
                    
                    deletedCount++;
                    
                    // Ограничиваем количество удалений за раз
                    if (deletedCount >= 5) break;
                    
                  } catch (error) {
                    console.log(`Ошибка при удалении ${release.tag_name}: ${error.message}`);
                  }
                }
              }
              
              console.log(`Удалено временных релизов: ${deletedCount}`);
              
            } catch (error) {
              console.log(`Общая ошибка при очистке: ${error.message}`);
            }
